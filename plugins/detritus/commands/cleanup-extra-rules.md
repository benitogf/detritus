---
description: Remove all detritus-generated rule files, hook scripts, and the matching settings.json hook entries. Leaves baseline settings (deny list, status line, effort, autoMode) untouched. Confirms before deleting.
---

The user invoked this command with: $ARGUMENTS

Call the detritus MCP tool `kb_get` with `name="flows/maintainer/cleanup-extra-rules"` and follow the returned guidance.
